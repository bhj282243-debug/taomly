# Taomly — зафиксированные версии зависимостей
# Обновлять через: pip install -U <package> && pip freeze > requirements.txt
# Проверять совместимость перед обновлением.

# ── Web Framework ────────────────────────────────────────────────────
fastapi==0.115.5
starlette==0.41.3  # Pinned by fastapi==0.115.5 (starlette CVEs require fastapi upgrade; see Phase 14 security backlog)
uvicorn[standard]==0.32.1
python-multipart==0.0.31
jinja2==3.1.6  # Phase 13: SSR templates for /r/{slug} public pages

# ── Database ─────────────────────────────────────────────────────────
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# ── Auth & Security ──────────────────────────────────────────────────
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==50.0.0  # Security: PYSEC-2026-35,1284,2141,3552,3553,3554,GHSA-537c (fix: 50.0.0)

# pyasn1: transitive dep of python-jose; python-jose 3.4.0 requires pyasn1<0.5.0
# PYSEC-2026-2263,3455,3456,3457 affect pyasn1; no compatible fix with python-jose 3.4.0
# Mitigation: upgrade to python-jose replacement (PyJWT) in future phase
# ecdsa 0.19.2: PYSEC-2026-1325 (no fix available, vendor monitoring)

# ── Telegram ─────────────────────────────────────────────────────────
pytelegrambotapi==4.23.0

# ── Rate Limiting ────────────────────────────────────────────────────
slowapi==0.1.9

# ── Monitoring ───────────────────────────────────────────────────────
sentry-sdk[fastapi]==2.19.2

# ── Config ───────────────────────────────────────────────────────────
python-dotenv==1.2.2

# ── HTTP Client (для telegram_service.py) ────────────────────────────
httpx==0.28.1

reportlab==4.2.5
email-validator==2.2.0

# ── Cloud Storage (Cloudflare R2 / S3-compatible) ────────────────────
boto3==1.35.76

# ── Image Processing ─────────────────────────────────────────────────
Pillow==12.3.0  # Security: PYSEC-2026-165,2249-2257,2874,3451-3496 (fix: 12.3.0)
